# Flight Tracker

Open the app web interface and visit **Admin** to configure the tracker latitude, longitude, search radius, and OpenSky credentials.

The primary OpenSky account is used normally. An optional backup account is used automatically after an OpenSky rate-limit response.

ADSBDB is used for aircraft metadata and route information. Its public GET API does not require a key.
